import { View } from 'react-native';

// This is a placeholder screen for the action tab button
// It should never be rendered as the tab button prevents navigation here
export default function ActionTab() {
  return <View />;
}